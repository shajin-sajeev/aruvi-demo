*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
# /vendor is intentionally NOT ignored — committed for Vercel deployment
# (Vercel's build container has no PHP, so composer install cannot run there)
# Run `composer install --no-dev` locally before pushing to update vendor/
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db
composer.phar
composer-setup.php
.composer-cache
.npm
